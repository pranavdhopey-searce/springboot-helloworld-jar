---
### Deploy Service ###

apiVersion: v1
kind: Service
metadata:
  name: springapp-service
  namespace: dev
spec:
  selector:
    app: springapp
  ports:
  - protocol: TCP
    port: 81
    targetPort: 8080
  type: LoadBalancer
